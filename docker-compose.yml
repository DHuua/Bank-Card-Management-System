version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: bankcards-postgres
    environment:
      POSTGRES_DB: TESTDB
      POSTGRES_USER: memory
      POSTGRES_PASSWORD: memoryishere1234
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - bankcards-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U memory -d TESTDB"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bankcards-app
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/TESTDB
      SPRING_DATASOURCE_USERNAME: memory
      SPRING_DATASOURCE_PASSWORD: memoryishere1234
    ports:
      - "8080:8080"
    networks:
      - bankcards-network
    restart: unless-stopped

volumes:
  postgres_data:

networks:
  bankcards-network:
    driver: bridge